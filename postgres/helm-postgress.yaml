architecture: standalone
global:
  postgresql:
    auth:
      password: d6FO3V9KfR824jz7
      postgresPassword: C4AJ91gA45EavECo
      username: admin
primary:
  persistence:
    ##existingClaim: postgres-pvc ##only if you already have existing PVC that want to be reattached
    enabled: true
    size: 2Gi
    storageClass: longhorn ##change to your specific storageclass
  resources:
    limits:
      cpu: 2
      memory: 2Gi
    requests:
      cpu: "0.5"
      memory: 0.3Gi
tls:
  autoGenerated: true
  enabled: true